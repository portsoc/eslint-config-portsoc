/* global QUnit */
'use strict';


QUnit.test(
  'test',
  function (assert) {
    assert.ok(1, 'foo');
  });


// todo test all the JS files in this repo
// todo test files in tests/files bad and good; shout if neither good or bad
